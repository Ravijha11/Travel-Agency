/** Product name and assets — keep in sync with manifest & metadata. */

export const BRAND_NAME = "Lahar connect";
export const BRAND_TAGLINE =
  "Shared rides between Lahar and Gwalior — find drivers and call instantly.";

/** Production website domain (used for canonical URLs and social cards). */
export const SITE_URL = "https://laharconnect.online" as const;

/** Public path (ASCII filename). File: `public/lahar-connect-logo.png`. */
export const LOGO_PATH = "/lahar-connect-logo.png" as const;

/** Home hero background (full-bleed banner behind the logo strip). */
export const HERO_BANNER_PATH =
  "/Firefly_Remove%20Hindi%20text%20of%20lahar%20keep%20Lahar%20In%20which%20is%20writtenn%20in%20english%20928299.png" as const;
